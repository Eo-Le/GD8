# check .github/workflows
url: "https://eo-le.github.io/GD8/"
baseurl: ""

# if path in root then in front matter use layout: default. 
# No need to define layout in root directory
defaults:
  - scope:
      path: ""
    values:
      layout: "default"

sass:
  load_paths:
    - _sass